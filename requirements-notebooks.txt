# Optional notebook + exploration stack (not installed by Render / CI).
# Use a separate venv if you like: python -m venv .venv-notebooks && source .../activate
#
#   pip install -e ".[dev]"
#   pip install -r requirements-notebooks.txt
#
# For catalog CPU builds with tight pins, prefer: pip install -e ".[catalog-build]"

jupyterlab>=4.4.8
notebook>=7.3.2
ipykernel>=6.29.5

kagglehub>=0.3.7
pandas>=2.2.3
numpy>=1.26.0,<2
matplotlib>=3.9.0
seaborn>=0.13.2
tqdm>=4.67.0

python-dotenv>=1.0.1
langchain>=0.3.18
langchain-community>=0.3.27
langchain-openai>=0.3
langchain-text-splitters>=0.3.9
langchain-chroma>=0.2.1

transformers>=4.53.0
torch>=2.2.0
